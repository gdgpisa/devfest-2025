---
import Base from '@/layouts/Base.astro'

import { ScheduleSection } from '@/components/preact/ScheduleSection'
import { TALKS } from '@/lib/sessionize'
---

<Base>
    <ScheduleSection client:only talks={TALKS} />
</Base>
